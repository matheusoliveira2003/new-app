// ----------- Slider Menu ----------
$('.slider').slick({
    dots: true,
    infinite: true,
    speed: 300,
    slidesToShow: 1,
    adaptiveHeight: true,
    autoplay: true,
    autoplaySpeed: 4000
  });
// ----------------------------------

// ---------- Modal Nova Vaga ---------
